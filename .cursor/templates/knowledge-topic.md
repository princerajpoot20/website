# {Topic Name}
> Created: {ISO date} | Updated: {ISO date}

## What this topic covers
{2–3 sentences. Scope of this topic within its parent chapter.}

## Key files
| File | Role |
|------|------|
| `{path}` | {what it does} |

## How it works
{The mechanism, end-to-end.

For a pipeline: inputs → each processing step in order → outputs. Name the function that performs each step.
For a subsystem: entry points → responsibilities → external calls it makes.

Enough that an agent reading only this page can trace behaviour without opening source files.}

## Configuration
{Env vars, config files, feature flags this topic reads or writes. Names only, never values.}

## Common gotchas
{Factual observations, not editorial. "The Fuse index is rebuilt every time a new tag is discovered" is useful. "Ordering could break here" is editorialising a fix and does not belong.}

## Related topics
{Other topics or chapters this interacts with, and how.}
